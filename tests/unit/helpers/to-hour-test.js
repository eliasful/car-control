
import { toHour } from 'car-control-mobile/helpers/to-hour';
import { module, test } from 'qunit';

module('Unit | Helper | to hour');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toHour([42]);
  assert.ok(result);
});

