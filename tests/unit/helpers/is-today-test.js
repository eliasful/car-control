
import { isToday } from 'car-control-mobile/helpers/is-today';
import { module, test } from 'qunit';

module('Unit | Helper | is today');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isToday([42]);
  assert.ok(result);
});

